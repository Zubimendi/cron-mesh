services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cronmesh
      POSTGRES_PASSWORD: cronmesh
      POSTGRES_DB: cronmesh
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cronmesh"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
